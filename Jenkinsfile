pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
               sh '''
               echo "Uploaded to git repository and fetch in Jenkins"
               '''
            }
        }
        
    }
}
